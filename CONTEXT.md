# Author Portal

The domain glossary moved to **knkcs/authorhub** (`CONTEXT.md` there) on
2026-08-20, merged with the seeded copy that had grown alongside the
implementation — no entry was lost. authorhub is the single source of truth
for decisions (its `docs/adr/` holds ADRs 0001–0010, moved the same day);
this repo is the archive.

This file's previous content stays in this repo's git history
(`git log -- CONTEXT.md`).
