# Decision record moved

The decision record — ADRs 0001–0010 — moved to **knkcs/authorhub**
(`docs/adr/` there) on 2026-08-20. authorhub is the single source of truth
for decisions, as it already is for the issue tracker; new ADRs are numbered
and filed there. This repo is the archive: research, specs, and the closed
v1 tickets.

The moved files' full history stays in this repo's git history
(`git log -- docs/adr/`).
